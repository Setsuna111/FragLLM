26-0120 djy
1. train_llama_addtoken.py设置TrainingArgs中dataloader_persistent_workers=True，ddp_timeout=10800，避免训练多个epoch时出现NCCL超时的问题；
2. 新增了模型权重merge的脚本
3. README.md中添加了训练脚本和权重merge脚本的说明
4. 更新了eval_grounding_addtoken.py中分母为0的小错误


26-0119 djy
1. 添加了训练过程中function/referring/grounding三种数据分别优化的训练脚本:train_llama_addtoken_alter, run_training_llama_addtoken_alter
2. 补充了Flash-Attn的方式（加速效果不明显，带宽不是主要瓶颈）
3. 补充了安装Flash-Attn的说明信息

25-1226 djy
1. 引入了pos_decoder_type("ProteinSAM" or "Simple")参数区分ProteinSAM decoder和Simple decoder两种架构方式
2. grounding任务构建conversation时，区分了create_conversation_sam(for protein_sam)和create_conversation_class(for simple decoder)
3. 修改了model_grounding_segformer导入函数的形式（如protein_sam.py中from .protein_encoder import ProteinEncoder）
4. 新增了protein_arch_addtoken_djy和protein_llama_addtoken_djy，基于simple decoder的架构
5. 新增了scripts/train_llama_addtoken.py，通过传入pos_decoder_type加载不同架构的模型进行训练
6. 调整了models/protein_arch_addtoken_lfj.py中load_protein_sam_params_with_overrides函数，新增了esm_model_path传参，确保初始化ProteinSAM时，ESM的路径与FragLLM中的路径保持一致，不受protein_sam_init_params.json中参数值的影响
7. 统一了eval，其中evaluate用来生成结果文件，metric文件用来计算指标结果。metric_grounding任务中，多positions匹配方式由中心距离匹配替换为了iou匹配
8. 新增了scripts/utils_argparse.py文件

1222 lfj
1. 增加prot2text权重load代码，加载adapter和llama的非embedding、lmhead权重。
    这里load的时候会分两部分加载，先加载llama权重，再加载adapter权重。当然这样分块儿加载后llama embedding和lmhead也可以同时读入
2. 合并了之前对referring任务的修改，支持传入frag_adapter参数选择仅从蛋白序列中获取fragment信息（qformer），和从蛋白序列+平均后的adapteroutput（因为在prot2text预训练和整体语义对齐了）中获取信息（multilevel）

1015 lfj
1. grounding decoder修改为transformer形式
2. grounding decoder没有提示输入时自动检测所有区域

1002 lfj 主要是优化代码
1. 修改baselines/blast_cls.py中查询方式，改为长序列为查询序列，短序列为数据库序列，加一下速
2. 删除了sample_rate这一个参数
3. 新增参数dataset_size，用以控制ProFunc dataset是否进行截断
4. 修改grounding任务的detailed_template的加载方式，不需要传入template这个参数了，此外，统一两个版本的template，最终以xx为准
5. 修改eval下文件格式，evaluate为首的文件作用为【得到并保存模型输出】，metric为首的文件作用为【根据模型输出计算指标】
6. 统一eval中ref任务和grounding的存储路径，现在是：
    ./eval
     --profunc
        --model_name_with_results_inside
     --referring_clsx
     --referring_desc
     --grounding_single
     --grounding_group
7. grounding decoder: 支持一键切换是分类还是分割训练得到的模型，支持传参设置读入哪一个预训练参数，支持保存预训练时的args，从而主模型在实例化时一键读入。

0922 lfj
1. 新增允许grounding模型的分类头参数可训练设置
2. 新增detailed_template，支持single和group两类训练任务

0918 lfj
1. 新增定位任务中相关baseline，详见baselines/blast_cls.py及sh，可调参数主要为evalue
2. 新增两个grounding任务的预训练模型，独立于两个文件夹中

0911 lfj
1. train_llama_lfj.py中新增loss检测代码，检测过大以及nan，追溯对应数据
2. models/protein_llama_lfj.py以及protein_arch_lfj.py中，新增使用门控机制融合global feature与residue feature的Q-former。
3. 修改dataloader_frag.py及dataloader_refferring.py，支持多个可学习查询向量。
4. 新增指代定位任务中的retrieval相关baseline，给定查询序列，返回数据库中相似性得分最高的一条，取其label为预测值。脚本见/baselines/{methods}.py，包括：
    alignment-based methods: blasy, tm-align, foldseek。
    sequence-based methods：plm.py支持huggingface model card的各种语言模型
    seq&struct-based models：saprot，单独涉及到结构token的转化
5. 修正ActRefDesc对data_name的定义
6. 修改zero2.json，避免过大grad_norm直接导致nan
7. dataset/dataloader_frag.py存在一些随机数bug的古战场遗迹，注意这些注释不要传到main

0910 djy
1. scripts/zero2.json中设置overlap_comm==false，避免训练过程中gradnorm出现nan

0909 djy
1. dataset/dataloader_grounding.py中删除了定位任务中位置区间前后的特殊token
2. eval/中添加了eval/evaluate_grounding.py，eval/evaluate_grounding.sh，eval/metric_group_grounding.py，eval/metric_group_grounding.sh，eval/metric_single_grounding.py和eval/metric_single_grounding.sh， 用于grounding任务的推理和指标计算

0905 djy
1. scripts/train_llama.py中注释了transformers.trainer_utils.set_seed(training_args.seed)
2. dataset/dataloader_refferring.py中修正了ActDesc任务数据加载时读取的数据集名称
3. eval/evaluate_function_desc.py中在写入结果时加入了dataset_idxs列，用于去除重复
4. eval/evaluate_desc_benchmark.py中对dataset_idxs列进行了去重
5. eval/evaluate_function_desc.py中设置了write_header判断条件，保证追加写入评估结果时不会添加header

0904 djy
1. 更新了requirements.text
2. README.md中添加了环境安装的流程
3. dataset/dataloader_frag.py中将HybridDataset中随机加载样本的方式更改为了根据索引加载样本
4. 修改了dataset/dataloader_grounding.py中对超长sequence的截断方式，当片段最大间距大于1021时，截断片段尾部；当序列长度大于1021，片段最大间距小于1021时，计算新序列起点的安全范围，保证片段不被截断，随后在安全范围内随机采样起点；当序列长度小于1021时，正常处理。

0904 lfj
1. 进一步修改了eval/evaluate_reference.py在多GPU并行时的数据问题。首先在dataset/dataloader_frag.py，dataset/dataloader_refferring.py，dataset/dataloader_function.py中添加了返回数据在数据集中dataset_idx的操作，随后于eval/evaluate_reference.py对多卡并行推理结果依据eval/evaluate_reference.py去重。
2. 由于测试数据集输出已经去重，故删除了evaluate_desc.py，evaluate_desc_benchmark.py中的去重代码，避免把（pred，label）相同的数据对剔除。
3. 修改了dataset/dataloader_refferring.py中对超长sequence的截断方式，当片段长度大于1021时，截断片段尾部；当序列长度大于1021，片段长度小于1021时，计算新序列起点的安全范围，保证片段不被截断，随后在安全范围内随机采样起点；当序列长度小于1021时，正常处理。
4. 删除了测试文件evaluate_reference_lfj.py，此外evaluate_function_desc.py/sh也可以删。

0903 lfj
1. 新增eval/evaluate_reference.py,eval/evaluate_reference.sh，合并prot2textinference任务的推理，支持单卡debug模式，并支持多文件评估。修改并行存储逻辑，解决了多卡并行存储时总数不对、保留列名重复。
2. trainer支持在training checkpoints时存储非lora参数，支持在resume_from_checkpoints时读入非lora参数，此处修改存储在train_llama_lfj.py，暂未修改到train_llama.py中
3. git merge使用说明：理想情况下，每一个开发分支只实现一个功能，开发结束后，及时合并入main，并checkout最新的main继续开发下一个功能。
4. 部分微信交换文件一并上传，包括：merge_lora_weights.py, merge_lora_weights_checkpoint.py, protein_arch.py对esm pooling layer的改动

0901_djy:修改内容
0. dataset/dataloader_frag.py中新增94-95，101-102，148-150行，适配description截断要求；
1. 调整models/protein_arch.py中182行FragmentAdapter初始化时，传入参数设错误的问题，没有config.adapter;
2. models/protein_llama.py中103行，将generate时prepare_inputs_labels_for_protein的无用传参设为None
3. merge_lora_weights.py 新增lora权重合并脚本
4. ./eval 新增description任务评估代码

0831 测试指标
1. ReferenceMetrics，统一的reference任务指标计算类，内含分类指标类和语言指标类
2. 支持多种输入
    1. Label-only: Traditional classification with pred_labels + target_labels
    2. InterPro text retrieval: LLM text descriptions + true InterPro IDs 
    3. Language metrics: Predicted texts + target texts for BLEU/ROUGE
    4. Combined: Any combination of the above for comprehensive evaluation

0830_djy:修改内容
0. protein_arch.py中调整了FragmentAdapter，统一了其调用过程，避免出现部分数据无需激活FragmentAdapter导致的计算图不一致的问题；
1. dataset/templates.py中扩充了referring和grounding任务的指令模板；
2. 额外添加了wandb/  sh_djy/  *.pyc  **/__pycache__/等文件的gitignore。


0830 修改+支持git
0. 支持使用git管理。0829云盘版本为初始main版本，再上传git之前，已通过compare folder同步所有修改。
1. 添加train_contrast_llama_allgather.py文件：
    （1）支持跨gpu对比学习
    （2）支持分minibatch推理
    （3）CosineAnnealing学习率衰减
    （4）对比策略改为取mean embedding
2. 修改模型参数加载：protein_arch.py line 199起两处，将keyword in k 修改为 '.' + keyword + '.' in k
3. git管理：
    （1）Ignored：/data /checkpoints /checkpoints_contrast /tensorboard_logs 这些文件需要本地复制或新建
    （2）若需要修改/尝试新东西，可以新建分支，确认尝试后merge到main中。若切换新服务器运行，记得pull一下main同步最新修改，科研助手上可能不是最新了
    （3）每次push具体的修改可以写到此文件中    


0829 修改
1. dataloader_grounding.py文件415行修改为 frags = data_item["fragments"]
2. scripts/train_llama.py 调整了一些参数的默认值（valid数据集默认为None）
3. models/protein_arch.py 226和227行调整为：
    inputs_embeds_old = self.get_model().get_input_embeddings()(input_ids)
    inputs_embeds = inputs_embeds_old.clone()
    避免lora训练时计算图出错
4.新增了scripts/fragllama_flash_attn_monkey_patch.py和scripts/train_mem.py，支持flashattention
5.stage2中batchsize调整为1（避免OOM）